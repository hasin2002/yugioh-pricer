# Accuracy-first server OCR

The first OCR implementation should favor recognition accuracy over the simplest TypeScript integration. Server-side OCR may use native tooling and preprocessing when that improves recognition of small Yu-Gi-Oh card text, while the capture client provides guidance when framing, blur, glare, or occlusion prevents reliable extraction.
