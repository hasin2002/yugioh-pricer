# Local-first Next.js, tRPC, Drizzle, and SQLite

The app will be built as a local-first web application using Next.js for the browser clients, tRPC for typed client-server calls, Drizzle for database access, and SQLite for local persistence. This stack fits the personal-use workflow while keeping the codebase deployable later without introducing cloud infrastructure or full authentication in the first version.
